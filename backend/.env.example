# MongoDB Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/achiever?retryWrites=true&w=majority

# JWT Secret Key (use a strong random string)
JWT_SECRET=your_super_secret_jwt_key_here_change_this

# Admin Invite Token (required to register as admin)
ADMIN_INVITE_TOKEN=your_admin_invite_token_here

# Client URL (frontend URL for CORS)
CLIENT_URL=http://localhost:5173

# Server Port
PORT=8000
